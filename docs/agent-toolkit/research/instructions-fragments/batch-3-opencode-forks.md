# Batch 3 — OpenCode+forks (raw research fragment)

> Raw output from research agent. Verdict labels use the brief's
> vocabulary; the orchestrator appends rows into `harness-matrix.md` as-is.

| Harness | Verdict | Default file | Project path / Global path | Reads AGENTS.md natively? | Mechanism | Citation | Reasoning |
|---|---|---|---|---|---|---|---|
| `opencode` | native | `AGENTS.md` | `./AGENTS.md` / `~/.config/opencode/AGENTS.md` | yes |  | https://opencode.ai/docs/rules/ | Project-root `AGENTS.md` auto-loaded with no config or flags; `~/.config/opencode/AGENTS.md` is the documented global override. Already satisfied — no pointer needed. |
| `crush` | native | `AGENTS.md` | `./AGENTS.md` / none (global-only) | yes |  | charmbracelet/crush `internal/config/config.go` — `defaultContextPaths` includes `AGENTS.md` / `agents.md` / `Agents.md`; `InitializeAs` JSON-schema `default=AGENTS.md` | Default context list auto-loads `AGENTS.md` (and variants) with no config; `/init` writes `AGENTS.md` by default. No documented user-config-dir auto-load (Issue #1050 tracks "Load user-level memory/context files" as still-open). |
| `goose` | native | `.goosehints` (and `AGENTS.md`) | `./.goosehints` and `./AGENTS.md` / `~/.config/goose/.goosehints` and `~/.config/goose/AGENTS.md` (XDG; on macOS `~/Library/Application Support/Block/goose/`) | yes |  | block/goose `crates/goose/src/hints/load_hints.rs` — `get_context_filenames()` defaults to `[".goosehints", "AGENTS.md"]`; `load_hints_from_directory` reads both project-cwd and `Paths::in_config_dir(...)` | Default loader concatenates both `.goosehints` and `AGENTS.md` at project + global scope with no config. AGENTS.md is loaded out-of-the-box without a pointer; `.goosehints` is co-loaded but doesn't shadow it. |
| `aider-desk` | unsupported (gap) | none (rule files toggled via UI / External Rules extension) | n/a — rule files live under user-chosen folders, enabled per-profile via UI | no |  | github.com/hotovo/aider-desk — README "Rule Files" / "External Rules extension"; no auto-loaded root file documented in README or main `AGENTS.md` | Aider-Desk does not auto-load a single root instruction file by default. Rule/conventions files are explicit assets selected in Settings → Agent Profiles or via the External Rules extension (folder picker). A same-name root symlink can't satisfy a UI-toggled, profile-scoped loader, and the underlying Aider engine itself only reads `CONVENTIONS.md` when wired in via `--read` / `.aider.conf.yml`. |

## What I checked — Batch 3 OpenCode+forks
- `opencode`: Read https://opencode.ai/docs/rules/ — confirms project `AGENTS.md` (traversed upward) and `~/.config/opencode/AGENTS.md` global, with `CLAUDE.md` only as a legacy fallback. Matches the prior; native confirmed.
- `crush`: Pulled `internal/config/config.go` from `main` on github.com/charmbracelet/crush — `defaultContextPaths` is a flat list (CLAUDE.md, GEMINI.md, crush.md variants, AGENTS.md/agents.md/Agents.md), all loaded if present (concatenated, not first-wins). `InitializeAs` JSON-schema defaults to `AGENTS.md`. No global/user-config auto-load yet — Issue #1050 ("Load user-level memory/context files") still open. Surprising vs priors: harness loads CLAUDE.md AND AGENTS.md by default, but verdict still `native` because AGENTS.md is in the default list and needs no pointer.
- `goose`: Pulled `crates/goose/src/hints/load_hints.rs` from `main` on github.com/block/goose — `get_context_filenames()` returns `[".goosehints", "AGENTS.md"]` unless overridden by `CONTEXT_FILE_NAMES`. `load_hints_from_directory` walks project + `Paths::in_config_dir(...)`, so both filenames are read at project AND global scope (XDG `~/.config/goose/` on Linux; `~/Library/Application Support/Block/goose/` on macOS per code comment). AGENTS.md prior reconfirmed; goose is unambiguously `native` now (`.goosehints` is co-loaded, not exclusive).
- `aider-desk`: Checked github.com/hotovo/aider-desk README and root `AGENTS.md`; READMEs describe a UI-managed Rule Files surface, Agent Profiles with explicit custom instructions, and an "External Rules extension" with user-picked folders. No code-path or doc statement that a root `AGENTS.md`/`CONVENTIONS.md` is auto-loaded with no config. Aider-desk embeds Aider, and upstream Aider itself only auto-loads `CONVENTIONS.md` if pointed there via `--read`/`.aider.conf.yml`. Logged as `gap`, not `by design`, because rule files are clearly first-class — they're just not a single default root file we can satisfy via a symlink.
