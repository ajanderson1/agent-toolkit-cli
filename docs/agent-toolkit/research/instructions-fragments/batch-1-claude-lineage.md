# Batch 1 — Claude-lineage (raw research fragment)

> Raw output from research agent. Verdict labels use the brief's
> vocabulary; the orchestrator appends rows into `harness-matrix.md` as-is.

| Harness | Verdict | Default file | Project path / Global path | Reads AGENTS.md natively? | Mechanism | Citation | Reasoning |
|---|---|---|---|---|---|---|---|
| `claude-code` | `symlink` | `CLAUDE.md` | `./CLAUDE.md` or `./.claude/CLAUDE.md` / `~/.claude/CLAUDE.md` | no | symlink | https://code.claude.com/docs/en/memory § "AGENTS.md" ("Claude Code reads `CLAUDE.md`, not `AGENTS.md`") | Docs state Claude Code reads `CLAUDE.md`, not `AGENTS.md`; `/init` only seeds CLAUDE.md from AGENTS.md, doesn't load it at runtime → fixed own-name file → symlink target. |
| `openclaw` | `native` | `AGENTS.md` | none (global/workspace-only) / `~/.openclaw/workspace/AGENTS.md` | yes | | https://docs.openclaw.ai/concepts/system-prompt + https://github.com/openclaw/openclaw README ("Injected prompt files: `AGENTS.md`, `SOUL.md`, `TOOLS.md`"; "Workspace root: `~/.openclaw/workspace`") | Personal-assistant model with single workspace at `~/.openclaw/workspace/`; auto-injects `AGENTS.md` (alongside `SOUL.md`/`TOOLS.md`) by default with no flags — but no per-project cwd file, so global slot only. |
| `kode` | `native` | `AGENTS.md` | `./AGENTS.md` (prefers `AGENTS.override.md`) / none (project-only; `~/.kode.json` is JSON config, not an instruction file) | yes | | https://github.com/shareAI-lab/Kode-cli README § "AGENTS.md Standard Support" ("Native support for the OpenAI-initiated standard format … prefers `AGENTS.override.md` over `AGENTS.md`") | Walks git-root → cwd reading `AGENTS.md` natively (and `AGENTS.override.md` first); legacy `CLAUDE.md` is also read for compat but `AGENTS.md` is the top-level default. Overturns prior — the README earned an AGENTS.md-Compatible badge. |
| `mux` | `native` | `AGENTS.md` | `<workspace>/AGENTS.md` / `~/.mux/AGENTS.md` | yes | | https://mux.coder.com/agents/instruction-files ("Mux picks the first matching base file: 1. AGENTS.md 2. AGENT.md 3. CLAUDE.md"; "Precedence: workspace … then global `~/.mux/AGENTS.md`") | First file in precedence is `AGENTS.md` at both project (`<workspace>/`) and global (`~/.mux/`) scopes; loaded by default with no flags or config. |
| `command-code` | `native` | `AGENTS.md` | `./AGENTS.md` / none (project-only — no documented user-level instruction file) | yes | | https://commandcode.ai/features § "AGENTS.md Project Memory" ("Define project-level instructions, code style guidelines, and architecture notes. Automatically loaded every session.") | Features page calls `AGENTS.md` the "Project Memory" file that is auto-loaded every session; `.commandcode/taste/taste.md` is an auto-written learned-preferences file, not the user-authored root instruction file. No global instruction-file path documented. |
| `codemaker` | `unsupported (by design)` | none | none / none | no | | https://github.com/codemakerai/codemaker-cli README ("Context-aware source code generation … Generating source code documentation … Fixing syntax"; usage is `codemaker generate docs **/*.java`) | Deterministic codegen CLI driven by per-invocation file-glob commands (`codemaker configure` + `codemaker generate …`); no notion of a runtime-loaded root instruction file / agent prompt context. |

## What I checked — Batch 1 Claude-lineage
- `claude-code`: Pulled the current memory doc at code.claude.com/docs/en/memory in this session — it has an explicit "AGENTS.md" subsection that states verbatim "Claude Code reads `CLAUDE.md`, not `AGENTS.md`" and recommends a symlink/import. Confirms the prior; `/init` is seed-only, runtime loader is `CLAUDE.md`/`CLAUDE.local.md` walking root→cwd. No upstream change vs prior.
- `openclaw`: Cross-checked openclaw/openclaw README and docs.openclaw.ai (System prompt + Plugins). Bootstrap markdown injected by default is `AGENTS.md`/`SOUL.md`/`TOOLS.md` from `~/.openclaw/workspace/`. Surprise vs typical model: OpenClaw is single-workspace ("personal AI assistant") — there is no per-cwd `./AGENTS.md` loader, so its native AGENTS.md slot lives at the global scope only. Verdict still `native` per the brief (reads AGENTS.md by default, no flags).
- `kode`: shareAI-lab/Kode-cli README in current `main` has an "AGENTS.md Standard Support" section with an "AGENTS.md Compatible" badge and prefers `AGENTS.override.md` over `AGENTS.md`; legacy `CLAUDE.md` is read for compat. Prior treated Kode as Claude-lineage CLAUDE.md — overturned: it is now natively AGENTS.md-first. Global config is `~/.kode.json` (JSON, not an instruction file), so global-scope instruction file is `none (project-only)`.
- `mux`: coder/mux is the relevant project (npm `mux` is unrelated). The official Instruction Files page (mux.coder.com/agents/instruction-files) gives explicit precedence `AGENTS.md` → `AGENT.md` → `CLAUDE.md` at both `<workspace>/` and `~/.mux/`. Cleanly `native` at both scopes — no surprises.
- `command-code`: commandcode.ai/features explicitly names "AGENTS.md Project Memory" as the auto-loaded project instruction file. `.commandcode/taste/taste.md` is a separate auto-written learned-taste artifact, not the user-authored root instruction. No global/user instruction file path is documented anywhere on the marketing/docs site I checked; recorded as `none (project-only)` pending docs.
- `codemaker`: codemakerai/codemaker-cli README (fetched via `gh api`) shows a deterministic codegen CLI with `codemaker configure` + `codemaker generate docs **/*.java`; no agent loop, no prompt context, no instruction file loader. Verdict `unsupported (by design)`.
