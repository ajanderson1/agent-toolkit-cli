# Batch 4 — Codex/OpenAI-likes (raw research fragment)

> Raw output from research agent. Verdict labels use the brief's
> vocabulary; the orchestrator appends rows into `harness-matrix.md` as-is.

| Harness | Verdict | Default file | Project path / Global path | Reads AGENTS.md natively? | Mechanism | Citation | Reasoning |
|---|---|---|---|---|---|---|---|
| `codex` | native | `AGENTS.md` | `<git-root>/AGENTS.md` (walks git-root → cwd) / `~/.codex/AGENTS.md` (or `AGENTS.override.md`; honors `$CODEX_HOME`) | yes |  | https://developers.openai.com/codex/guides/agents-md | Codex builds an instruction chain from `~/.codex/AGENTS.md` plus every `AGENTS.md` from git-root down to cwd at every session start with no flags; 32 KiB cap. |
| `kimi-cli` | native | `AGENTS.md` | `<git-root>/AGENTS.md` (walks git-root → cwd, plus `.kimi/AGENTS.md`) / none (global-only AGENTS.md not documented; user config lives at `~/.kimi/config.toml`) | yes |  | https://moonshotai.github.io/kimi-cli/en/release-notes/changelog.html (v1.29.0 2026-04-01) | v1.29.0 ships hierarchical AGENTS.md discovery merged from git-root to cwd (incl. `.kimi/AGENTS.md`), injected as `${KIMI_AGENTS_MD}` system-prompt variable; runs automatically with no flags. |
| `dexto` | native | `AGENTS.md` (priority over `CLAUDE.md`, then `GEMINI.md`) | `<workspaceRoot>/AGENTS.md` / none (global-only AGENTS.md not auto-loaded; only `~/.dexto/commands/` is global) | yes |  | https://github.com/truffle-ai/dexto/blob/main/packages/agent-management/src/config/discover-prompts.ts (`AGENT_INSTRUCTION_FILES = ['agents.md', 'claude.md', 'gemini.md']`, `discoverAgentInstructionFile`) | `enrichAgentConfig` runs at CLI startup and calls `discoverAgentInstructionFile(workspaceRoot)`; `agentFile.discoverInCwd` defaults to `true`; AGENTS.md is the first-priority file added as a system-prompt file-contributor. |
| `neovate` | native | `AGENTS.md` (project also walks for `CLAUDE.md`, `NEOVATE.md`; global also checks `NEOVATE.md` and `~/.claude/CLAUDE.md`) | `<cwd>/AGENTS.md` walking up to filesystem root / `~/.neovate/AGENTS.md` | yes |  | https://github.com/neovateai/neovate-code/blob/master/src/rules.ts (`getLlmsRules`, `projectRuleNames`/`globalRuleNames` starting with `'AGENTS.md'`) | `src/rules.ts:getLlmsRules` walks from cwd up to root reading `AGENTS.md` first at each level, then `~/.neovate/AGENTS.md`; all loaded by default into `llmsDescription` with no flags. |

## What I checked — Batch 4 Codex/OpenAI-likes
- `codex`: Re-verified the prior. Read official guide at `developers.openai.com/codex/guides/agents-md` — Codex auto-discovers and merges `~/.codex/AGENTS.md` + every `AGENTS.md` from git-root to cwd, capped at 32 KiB. `AGENTS.override.md` precedes `AGENTS.md` at each level. No flags required. Prior confirmed.
- `kimi-cli`: Prior absent. Issue #850 (auto-load AGENTS.md feature request) is CLOSED — initially looked unimplemented, but the v1.29.0 changelog entry (2026-04-01) confirms hierarchical AGENTS.md discovery shipped, including `.kimi/AGENTS.md` and the `${KIMI_AGENTS_MD}` system-prompt injection variable. Global-scope AGENTS.md (e.g. `~/.kimi/AGENTS.md`) is not documented as auto-loaded.
- `dexto`: Prior absent (suspected `unsupported (by design)` from docs which only describe YAML system prompts). Source-code check overturned that: `packages/agent-management/src/config/discover-prompts.ts` defines `AGENT_INSTRUCTION_FILES = ['agents.md', 'claude.md', 'gemini.md']`, and `config-enrichment.ts` calls `discoverAgentInstructionFile(workspaceRoot)` with `discoverInCwd` defaulting to `true`. AGENTS.md is the highest-priority file, project-scope only — global is for commands, not instructions.
- `neovate`: Prior absent. Found definitive loader at `src/rules.ts` (`getLlmsRules`): `projectRuleNames = ['AGENTS.md', 'CLAUDE.md', '<PRODUCT>.md']` walked from `cwd` up to filesystem root, then `globalRuleNames = ['AGENTS.md', '<PRODUCT>.md']` from `~/.neovate/` plus a fallback to `~/.claude/CLAUDE.md`. AGENTS.md is the first-checked name at both scopes; runs by default with no flags.
