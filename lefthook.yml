pre-commit:
  parallel: true
  commands:
    pytest:
      run: uv run pytest -q
      stage_fixed: false
    schema-vendor-check:
      run: diff schemas/asset-frontmatter.v1alpha2.json src/agent_toolkit_cli/_schemas/asset-frontmatter.v1alpha2.json
      stage_fixed: false
