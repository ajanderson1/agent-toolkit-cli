"""Bundled package data.

Holds the harness matrix shipped into the wheel (and sdist) so runtime code can
read it via importlib.resources on packaged installs — see
src/agent_toolkit_cli/commands/instructions/list_cmd.py and #305.
"""
