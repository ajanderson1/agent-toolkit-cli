# Visual judgment — #480

- **Palette / Settings:** PASS — `ctrl+p` shows a readable Settings result; the modal is centred, labelled, and usable.
- **Live theme + draft split:** PASS — Nord visibly applies behind the still-open modal; the persistence copy makes the immediate-theme / draft-harness split clear.
- **Pi selection:** PASS — Pi is absent from captured Skills, Agents, Commands, and global MCP columns; the restart capture preserves that state.
- **Cursor / empty selection:** PASS — Cursor’s full “Standard-covered; no standalone column to hide” copy is visible; empty selection retains a readable Skills + Standard + State + Source grid.
- **Bad / unknown state:** PASS — malformed JSON and `ghost-harness` both show named, readable status-bar notices; the two-row status bar leaves a real text row below its border.

**Overall: PASS** — no clipping, overlap, or unreadable required state in the inspected PNGs.
